server.port=8080
spring.h2.console.enabled=true
spring.datasource.url=jdbc:h2:file:./backend/data/serviceportal;DB_CLOSE_ON_EXIT=FALSE;AUTO_RECONNECT=TRUE
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Allow CORS from frontend (Vite default)
# Note: further CORS config may be added if needed
